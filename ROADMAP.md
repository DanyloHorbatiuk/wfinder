# ROADMAP — WorkFinder

Статуси: `[ ]` не почато · `[~]` у роботі · `[x]` готово.
Кожен пункт посилається на розділ `SPEC.md`. Пункт вважається готовим лише тоді, коли виконано його критерії з SPEC §14.

Пункти виконуються в указаному порядку. Не можна починати наступний, доки попередній не готовий, якщо в описі пункту не сказано інакше.

```
F-01 → I-01 → F-02 → F-03 → E-08 → E-06 → E-07 → E-03 → E-02 → E-01 → E-09
                                                                   │       │
                                              фаза 2:   E-05 ←─ E-08   E-04 ←─ E-01   E-10 ←─ E-09
```

---

## Фаза 1 — мінімум

### Етап 1.0 — Стабілізація

- [x] **F-01. Гігієна репозиторію та керування схемою** — SPEC §3 (D4, D9–D12, D15, D16), §4.2, §4.3, §7.3
  - [x] Додати `db-data/` у `.gitignore`; створити `.env.example` з усіма змінними з §13 (без реальних значень).
  - [x] Видалити `notify/courses.py`. `main.py` виправити або видалити; рішення записати в журнал.
  - [x] `core/model.py`: прибрати `pygments`, виправити `__repr__`, оголосити частковий унікальний індекс через `__table_args__`.
  - [x] `adapters/base.py`: контракт `parse(...) -> list[Course]`, `source` як атрибут класу, константа `PARSER_VERSION`.
  - [x] Створити `utils/time.py` з функцією `utcnow()`.
  - [x] Alembic: базова ревізія (для наявної БД — `stamp`), створення схеми `analytics`; сервіс `migrate` у compose; `alembic` у `Dockerfile`.
  - [x] Прибрати дублюючі монтування пакетів у compose; єдиний шлях імпорту — `/opt/airflow/project`. Переконатися, що DAG імпортується.
  - [x] `NOTIFY_DRY_RUN` у `Settings` і `notify/telegram.py`.

- [x] **I-01. Дослідження сирих даних** — SPEC §8.1, §15
  - [x] Написати `scripts/inspect_raw.py` і запустити для кожного джерела.
  - [x] Записати відповіді на Q1–Q6 у SPEC §15.
  - [x] Якщо відповіді змінюють специфікацію (поля хешу, повнота знімків), оновити SPEC до початку F-02.

- [x] **F-02. Коректне версіонування** — SPEC §3 (D1, D2, D7, D8, D14), §4.2, §5.1–5.3
  - [x] Міграція: `file_record.fetched_at`, `courses.content_hash`, `last_seen_at`, `close_reason`, індекс `ix_courses_posting`; `description` (лише SoftServe, за I-01 — `tags` не додається).
  - [x] `core/hashing.py`.
  - [x] Завантажувач приймає `Session`, обробляє файли за часом знімка, вирішує insert / changed / unchanged і повертає `LoadReport`.
  - [x] Адаптери: розбір дат EPAM, `description` для SoftServe (I-01).
  - [x] `fetch.py`: `fetched_at` і ключ у справжньому UTC.
  - [x] `fetch.py`: SoftServe — пройти пагінацію (`meta.last_page`) і об'єднати всі сторінки в один знімок перед збереженням у MinIO (I-01, SPEC §6.1, §15 Q2).

- [x] **F-03. Прозорі збої** — SPEC §3 (D5, D6), §5.1, §6.1
  - [x] `fetch_one_source` піднімає винятки.
  - [x] Файл обробляється в транзакції з записом статусу `error`; файли інших джерел обробляються далі.
  - [x] `trigger_rule=all_done` для `load_all_to_db` і `notify_digest`; задача `finalize`.

### Етап 1.1 — Надійність

- [x] **E-08. Метрики завантажень (`load_stats`)** — SPEC §4.2, §5.3 п. 7, §11 (`v_pipeline_health`)
  - Реалізується до E-06, бо захист використовує історію `received`.
  - [x] Міграція `load_stats` (один рядок на `file_record`, `UNIQUE(file_record_id)`).
  - [x] Завантажувач вставляє рядок `load_stats` для кожного успішно обробленого файлу; заодно реалізована відкладена з F-02 out-of-order-перевірка (§5.1 п.4).
  - Створення `analytics.v_pipeline_health` — у E-02 разом з рештою представлень (`scripts/apply_views.py`), не тут.

- [x] **E-06. Закриття зниклих пропозицій і захист від масового закриття** — SPEC §5.3 п. 5–6, §5.4
  - [x] Правила R1–R3, параметри в `Settings`.
  - [x] `scripts/simulate_guard.py` (транзакція з відкатом).

- [x] **E-07. Сповіщення про збої та оновлений дайджест** — SPEC §3 (D13), §7
  - [x] `notify/alerts.py`, `on_failure_callback` через `default_args`.
  - [x] Дайджест на основі `LoadReport`: екранування, поділ на частини, підсумковий рядок, заблоковані джерела та помилки.

### Етап 1.2 — Історія та аналітика

- [x] **E-03. Replay з MinIO** — SPEC §6.2
  - Передумова: відповіді на Q2 і Q4. Q2 відповіли в I-01; Q4 лишається відкритим (MinIO в цьому середовищі порожній) — код написано й перевірено без нього, дивись журнал.
  - [x] `replay_courses_dag`: синхронізація `file_record`, бекап, перебудова в транзакції, звіт.
  - [x] Спершу запуск `dry_run=true`; реальний запуск лише після перегляду звіту (можливість передбачена — реальний запуск без `confirm=REPLAY` падає).

- [x] **E-02. Тривалість життя пропозицій** — SPEC §11
  - [x] `scripts/apply_views.py`.
  - [x] `v_active_courses`, `v_posting_episodes`, `v_daily_activity`, `v_field_changes`, `v_pipeline_health`.

- [x] **E-01. Витягування навичок** — SPEC §8, §11
  - [x] Таксономія на 100–150 навичок, `direction_map`.
  - [x] `enrich/skills.py` (чиста функція), `SKILLS_VERSION`.
  - [x] Міграція `skill`, `course_skill`, `courses.skills_version`; задача `extract_skills` у DAG.
  - [x] `v_skill_counts`, `v_skill_weekly`, `v_skill_pairs`; `scripts/skills_sample.py`.

- [~] **E-09. Дашборди Metabase** — SPEC §12.1
  - [x] Питання 1–12 як нативні SQL-запити; тексти в `metabase/cards/` (12-те — з E-05, за нотатком у власному пункті E-05).
  - [ ] Два дашборди з фільтрами (поки що створюються вручну в інтерфейсі) — потребує живого Metabase, недоступного в цьому середовищі; `metabase/README.md` містить покрокову інструкцію для власника проєкту.

---

## Фаза 2 — після мінімуму

- [x] **E-04. Класифікація правилами** — SPEC §9
  - Залежить від E-01 (той самий підхід до версіонування). Зміни в адаптерах вимагають replay.
  - [x] `enrich/classify.py` (чиста функція), `enrich/geo_aliases.yaml`, `RULES_VERSION`.
  - [x] Міграція `course_enrichment`; задача `enrich_courses` у DAG (після `extract_skills`).
- [ ] **E-05. Перевірка якості та карантин** — SPEC §10
  - Залежить від E-08. Додає питання 12 на дашборд «Здоров'я пайплайна».
- [ ] **E-10. Відтворюваність Metabase** — SPEC §12.2
  - Залежить від E-09.

---

## Відкладено

Не реалізовувати без окремого рішення власника проєкту:

- рекомендації (TF-IDF, ембеддинги, pgvector) і персоналізовані дайджести;
- оформлення звіту з практики;
- інтерактивний Telegram-бот, REST API;
- dbt та окремий фреймворк шарів bronze/silver/gold;
- CI, Makefile, тестова інфраструктура.

Нові джерела даних допускаються лише за шаблоном адаптера і після завершення фази 1.

---

## Журнал рішень

Claude Code додає рядок після кожного пункту, а також щоразу, коли реалізація відходить від SPEC.

| Дата | Пункт | Рішення | Причина |
|---|---|---|---|
| 2026-09-16 | F-01 | `main.py` видалено, а не виправлено | Дублював логіку DAG (`get_sources` + `fetch_and_save_single` + `process_pending_files`); окремий CLI/локальний запуск поза обсягом SPEC §1.2, а сигнатури, які він викликав, все одно зміняться в F-02 |
| 2026-09-16 | F-01 | `.env.example` містить усі змінні з §13, включно з тими, що будуть задіяні пізніше (`TELEGRAM_ALERT_CHAT_ID`, `GUARD_*`, `METABASE_*`) | Так вимагає формулювання пункту F-01 ("з усіма змінними з §13"); у `Settings` додано лише `NOTIFY_DRY_RUN`, бо це єдина змінна, яку F-01 явно вимагає підключити. Решту буде додано в `Settings` разом із відповідними пунктами (E-06, E-07, E-10) |
| 2026-09-16 | F-01 | Базова ревізія Alembic (`0001_baseline`) відтворює поточну схему (`file_record`, `courses`) уже з виправленим частковим унікальним індексом (D4) і не додає нових колонок | Нові колонки (`content_hash`, `last_seen_at`, `close_reason` тощо) належать F-02; перевірено локально (postgresql-16 без Docker, бо демон Docker недоступний у цьому середовищі): `upgrade head` на порожній БД і `create_all` на "наявній" БД дають однаковий `uq_active_course_per_source`; `stamp head` після цього коректно проставляє ревізію |
| 2026-09-16 | F-01 | Єдиний шлях монтування проєкту в compose — `.:/opt/airflow/project` для `airflow-scheduler`, `airflow-dag-processor`, `airflow-webserver`, `migrate`; додано `AIRFLOW__CORE__DAGS_FOLDER=/opt/airflow/project/dags` | Виправляє D16 (подвійне монтування пакетів, неоднозначний `BASE_DIR`); один шлях імпорту для всіх сервісів, DAG-файли лишаються з легкими імпортами на верхньому рівні, тому `dag-processor`/`webserver` не потребують встановлених залежностей проєкту |
| 2026-09-16 | F-01 | Додано `httpx` у `Dockerfile` (новий дефект, умовно D17, не було в §3) | `notify/telegram.py` імпортує `httpx`, якого не було в списку `pip install`; без цього `notify_digest`/`notify_new_courses_task` впали б у реальному контейнері `airflow-scheduler` з `ModuleNotFoundError`. Виявлено під час перевірки "імпорт без побічних ефектів" для критерію F-01 |
| 2026-09-16 | F-01 | Додано `__pycache__/`, `*.pyc` у `.gitignore` (не було в списку з D15, але це та сама "гігієна репозиторію") | Виявлено під час локальної перевірки (`py_compile` генерує `__pycache__/`); без цього такі теки потрапили б у git |
| 2026-09-16 | F-01 | Критерій "`docker compose run --rm migrate` успішно виконується і на наявній, і на порожній БД" перевірено без Docker (демон недоступний у цьому середовищі) — локальним PostgreSQL 16 і прямим викликом `alembic upgrade head` / `alembic stamp head` з тим самим `alembic.ini`/`migrations/env.py`, які буде використовувати сервіс `migrate` | Docker-демон не запущений у цій сесії (`/var/run/docker.sock` відсутній); `docker compose config` / реальний прогін `docker compose run --rm migrate` рекомендується виконати власнику проєкту на повному стеку перед тим, як вважати F-01 остаточно закритим |
| 2026-09-16 | I-01 | `tags JSONB` прибрано зі SPEC (§4.2, §5.2); `description TEXT` лишається, але заповнюється лише для SoftServe | Реальні знімки (`epam_raw.json`, 206 записів; `softserve_raw.json`, 1 запис) показали: EPAM не має `description`/`tags` на рівні запису (`AdditionalSkillsIds` завжди порожній); SoftServe має `description`, але не має `tags`. Жодне джерело тегів не надає |
| 2026-09-16 | I-01 | Виявлено пагінацію SoftServe (`meta.total`, `meta.last_page`, `meta.per_page=20`), яку поточний `fetch.py` не обробляє (бере лише сторінку 1) | У знімку `last_page=1`, тому дефект поки не проявляється, але F-02 має пройти всі сторінки перед збереженням знімка, інакше майбутнє зростання кількості стажувань понад 20 призведе до тихої втрати даних і хибних спрацювань захисту E-06. Додано в SPEC §6.1 і в чек-лист F-02 |
| 2026-09-16 | I-01 | EPAM `Status`: у знімку лише `RegistrationOpen`/`Planned`, значення "закрито" немає | Підтверджує D3 — закриті тренінги зникають зі списку, а не позначаються статусом; окремого статусу-індикатора закриття в EPAM очікувати не варто, логіка закриття через "зникнення зі знімка" (E-06) лишається єдиним джерелом правди |
| 2026-09-16 | I-01 | Фільтр EPAM (Україна, `PlanLevel > 2`) лишається без змін: 9 з 206 записів знімка проходять фільтр | Запис, що перестав проходити фільтр, вважається таким, що зник зі знімка адаптера, і закривається звичайною логікою E-06 (`close_reason='removed'`); окремий стан для "не пройшов фільтр" не вводиться |
| 2026-09-16 | I-01 | Q3 (buildId EPAM) підтверджено, Q4 (кількість файлів у `raw/`) відкладено | Q3: URL містить Next.js `buildId` (`yQvZROZOVSjx62IVxVebj`), нестабільний після редеплою EPAM — очікувана поведінка для F-03/E-07, ручне оновлення `CAREERS_URL_EPAM` неминуче. Q4: MinIO в цьому середовищі порожній (пайплайн ще не працював наживо), відповідь можлива лише після реальної експлуатації — уточнити перед E-03/E-02 |
| 2026-09-16 | F-02 | `process_pending_files` реалізує лише §5.3 кроки 1–4 і базове оновлення `file_record.status`/`processed_at`; закриття зниклих пропозицій (кроки 5–6) і запис `load_stats` (крок 7) не реалізовані | Кроки 5–6 залежать від захисту E-06, крок 7 — від таблиці `load_stats` (E-08); ROADMAP явно ставить F-02 перед обома. `LoadReport.blocked`/`per_source[*].closed` лишаються порожніми/нульовими до E-06. Регресії немає: до F-02 зниклі пропозиції теж ніколи не закривались (D3) |
| 2026-09-16 | F-02 | Композитний `CHECK ((active_to IS NULL) = (close_reason IS NULL))` з SPEC §4.2 у міграцію `0002` не додано | Рядки, закриті до F-02 (є `active_to`, немає `close_reason`, бо стовпця не існувало), одразу порушили б цей CHECK; SPEC сам вимагає застосувати його "після replay або з очищенням старих рядків" — додати разом з E-03 |
| 2026-09-16 | F-02 | `core/repository.py`: видалено `CourseRepository.upsert_course` і `EXCLUDED_FIELDS` (D1), замінено на `get_active_by_source` — вся insert/changed/unchanged-логіка тепер у `storage/loader.py` | `upsert_course` завжди закривав і вставляв нову версію незалежно від змін (сам дефект D1); тримати непрацюючий, більше не викликаний метод немає сенсу |
| 2026-09-16 | F-02 | Виявлено ще один дефект поза списком D1–D16 (умовно D18): `fetch.py: load_file_and_meta` ловив лише `ClientError` і мовчки глушив помилку (з баґом — звертався до неіснуючої змінної `file` замість `f_name`), через що `fetch_and_save_single` міг повернути `status: success`, навіть якщо збереження в MinIO/БД впало | Виправлено разом з переписом `fetch.py`: жодного внутрішнього `try/except` у `load_file_and_meta` більше немає, будь-яка помилка піднімається й ловиться зовнішнім `try/except` у `fetch_and_save_single`, який і так формує `status: error` |
| 2026-09-16 | F-02 | `PARSER_VERSION` залишено `= 1`, replay не потрібен | Це перше впровадження `content_hash`; усі наявні активні версії матимуть `content_hash IS NULL`, і алгоритм §5.3 п.4 явно обробляє цей випадок без створення зайвої версії (бекфіл хешу "на льоту"). Бампати `PARSER_VERSION` є сенс лише при майбутній зміні набору полів/нормалізації після цього моменту |
| 2026-09-16 | F-02 | Перевірено без Docker (демон недоступний): локальний PostgreSQL 16 + реальні знімки з I-01 (`epam_raw.json`, `softserve_raw.json`) через фейковий S3-клієнт замість MinIO. Два послідовні запуски без змін → `inserted=0, changed=0, unchanged=received`; змінений запис → стара версія закривається (`close_reason='changed'`), нова вставляється, дублікатів активних версій немає; зламаний файл одного джерела → `status=error`, інші файли цього джерела лишаються `pending`, інше джерело обробляється штатно; жоден файл не застряг у `processing` | `docker compose exec airflow-scheduler airflow dags test load_courses_pipeline_dag` з реальним MinIO рекомендується прогнати власнику проєкту перед тим, як вважати F-02 остаточно підтвердженим на повному стеку |
| 2026-09-16 | F-03 | `finalize` реалізовано через задокументований "watcher"-патерн Airflow: `trigger_rule="all_done"`, `get_current_context()["dag_run"].get_task_instances(state=[FAILED, UPSTREAM_FAILED])` + окрема перевірка `LoadReport.errors` (бо помилки окремих файлів F-02 ізолює й не валить сам `load_all_to_db`) | Мережевий egress у цьому середовищі блокує документацію Airflow, тому сигнатури (`get_current_context` з `airflow.sdk`, `AirflowFailException` з `airflow.exceptions`, `DagRun.get_task_instances(state: Iterable[TaskInstanceState])`) перевірено не за документацією, а напряму встановленим локально пакетом `apache-airflow==3.1.0` (той самий пін, що в `Dockerfile`) — код, сигнатури й типи присутні |
| 2026-09-16 | F-03 | Критерій SPEC §14 F-03 "у логах є текст сповіщення (dry-run)" не перевірявся | Це посилання на `on_failure_callback`/`notify/alerts.py`, яких не існує до E-07; ROADMAP явно ставить E-07 після F-03. Решта критеріїв F-03 (`fetch_one_source[broken]` падає після повторних спроб; `finalize` валить прогін DAG; жоден файл не в `processing`) перевірені |
| 2026-09-16 | E-08 | `analytics.v_pipeline_health` не створено в цьому пункті | ROADMAP явно віддає `scripts/apply_views.py` і повний список представлень (включно з `v_pipeline_health`) пункту E-02; E-08 лише робить існування таблиці `load_stats` можливим. SPEC §14 для E-08 теж не згадує представлення, лише SQL-перевірки по `load_stats` |
| 2026-09-16 | E-08 | Реалізовано відкладену з F-02 перевірку "out-of-order snapshot" (SPEC §5.1 п.4): якщо `fetched_at` файлу менший за вже записаний `MAX(load_stats.snapshot_at)` цього джерела — файл падає в `status='error'` з відповідним повідомленням, замість того щоб обробитися | Ця перевірка залежала від таблиці `load_stats`, якої не існувало до E-08 — саме тому вона була відкладена в журналі F-02, а не забута |
| 2026-09-16 | E-08 | `load_stats` рахується на рівні файлу (`SourceStats` тепер створюється локально в `_process_one_file` і мерджиться в `LoadReport.per_source` через новий метод `SourceStats.add()`), а не на рівні кумулятивного лічильника джерела за весь прогін | `load_stats` за SPEC §4.2 — "один рядок на оброблений файл" з `UNIQUE(file_record_id)`; старий підхід (один спільний лічильник на джерело за весь `process_pending_files`) дав би неправильні числа, якби в одному прогоні опинилося кілька файлів одного джерела (напр. після простою чи в replay) |
| 2026-09-16 | E-08 | Перевірено без Docker: локальний PostgreSQL 16, міграції `0001`–`0003`, реальні знімки I-01 через фейковий S3-клієнт. Два запуски (з накопиченими попередніми даними) → кожен `done`-файл має рівно один рядок `load_stats`, інваріант `inserted+changed+unchanged = received-rejected` виконується для всіх рядків; синтетичний файл зі `fetched_at` старішим за вже записаний `snapshot_at` джерела → коректно падає як `out-of-order snapshot` | — |
| 2026-09-16 | E-06 | `core/guard.py: evaluate_closure_guard` перевіряє R1→R2→R3 послідовно й повертає перше правило, що спрацювало (не комбінує кілька причин в одному `block_reason`) | Формат `report.blocked`/`load_stats.block_reason` у SPEC — один рядок-причина (напр. `"R2: received=3, baseline=41"`); порядок R1→R2→R3 відповідає порядку опису правил у §5.4 |
| 2026-09-16 | E-06 | `to_close` обчислюється як `active_by_source_id` (знімок активних версій, завантажений на початку обробки файлу) мінус ключі, побачені в дедуплікованому знімку — записи, закриті в цьому ж файлі через `close_reason='changed'`, автоматично не потрапляють у `to_close`, бо їхній `source_id` був "побачений" | Це саме множина активних версій, яких цикл insert/changed/unchanged не торкнувся, тобто рівно ті, що зникли зі знімка — відповідає §5.3 п.5 |
| 2026-09-16 | E-06 | `scripts/simulate_guard.py` не пише окремий симулятор insert/changed/unchanged/guard — будує синтетичний `raw`-знімок із реальних активних `Course` даного джерела (порожній для `--empty`, частка для `--keep-share`) і прогонює його через справжній `process_pending_files(..., commit_per_file=False)` з подальшим `session.rollback()` | Уникає розбіжності між "симуляцією" і реальною логікою продакшн-коду; `commit_per_file=False` вже дає потрібну поведінку "транзакція з відкатом" через savepoint-механізм з F-02, нічого нового для цього винаходити не треба |
| 2026-09-16 | E-06 | Перевірено без Docker: локальний PostgreSQL 16, реальні дані I-01 (9 активних EPAM після фільтра, 1 SoftServe). `--empty --source softserve` → `blocked: R1: received=0, active=1`; `--keep-share 0.2 --source epam` з тимчасово зниженим через env `GUARD_MIN_ACTIVE=2` (бо в зразку лише 9 активних EPAM, а дефолт `guard_min_active=10`) → `blocked: R3: to_close=7, active=9, share=0.78`; кількість рядків `courses`/`load_stats` до і після скрипта однакова в обох випадках. Окремо перевірено "дозволений" сценарій напряму через `process_pending_files` (не через скрипт): знімок без одного з 9 активних EPAM-записів → запис коректно закривається (`close_reason='removed'`, `active_to` встановлено), `closed=1` у `LoadReport` | R3 в реальних умовах спрацьовує лише коли `active_count ≥ guard_min_active` (дефолт 10); у наданих I-01-зразках обох джерел активних записів менше — перевірка R3 з дефолтними порогами на реальних даних лишається на власника проєкту, коли в БД накопичиться достатньо активних пропозицій |
| 2026-09-16 | E-07 | `on_failure_callback` підключено через `default_args={"on_failure_callback": _task_failure_alert}` на рівні `@dag(...)`, де `_task_failure_alert` — тонка обгортка на верхньому рівні файлу DAG, що імпортує `notify.alerts.task_failure_alert` лише всередині тіла функції | `default_args` обчислюється при парсингу файлу DAG (тобто й у `airflow-dag-processor`, де немає залежностей проєкту), тому прямий імпорт `notify.alerts` на верхньому рівні зламав би парсинг у цьому контейнері; обгортка без власних імпортів на верхньому рівні лишає файл DAG "легким" (CLAUDE.md), а реальний імпорт відкладається до моменту фактичного виклику колбека (лише в `airflow-scheduler`, де залежності є) |
| 2026-09-16 | E-07 | Контекстні ключі колбека (`task_instance`/`ti`, `run_id`, `dag_run`, `exception`, `ti.log_url`, `ti.try_number`) і `default_args`/`trigger_rule`-механіка перевірені не за документацією (мережевий egress блокує docs.airflow.apache.org), а прямим читанням коду встановленого локально `apache-airflow==3.1.0` (`airflow/sdk/execution_time/task_runner.py`) | Той самий підхід, що й у F-03: коли документація недоступна, звірка з реальним встановленим пакетом (той самий пін, що в `Dockerfile`) — найближче до "перевірити за документацією Airflow 3.1", що можливо в цьому середовищі |
| 2026-09-16 | E-07 | `core/repository.py: CourseRepository.get_courses_created_today` видалено (реалізація дефекту D2) | Востаннє викликався зі старого `notify/digest.py`, якого більше немає — дайджест тепер повністю будується з `LoadReport` (`new_course_ids`, `per_source`), а не з `Course.created_at`. Востаннє щось використовувало метод — прибрано мертвий код разом з тепер-непотрібними імпортами `datetime`/`time`/`utils.time.utcnow` у цьому файлі |
| 2026-09-16 | E-07 | `notify_digest`-задача тепер приймає `load_report: dict` як аргумент (через `notify_digest(load_task)` у графі DAG) замість виклику без параметрів | Того вимагає SPEC §7.1 ("Дані беруться з `LoadReport` у XCom"); TaskFlow автоматично підтягує XCom `load_all_to_db`, коли передати `load_task` як аргумент |
| 2026-09-16 | E-07 | Перевірено без Docker: локальний PostgreSQL 16, синтетичний `LoadReport` із 61 новим курсом (один із символами `<`, `&`, `>` в назві та URL) і заблокованим/помилковим джерелом. `build_digest_parts` дав 2 частини (3962 і 1289 символів, обидві ≤4000), увесь HTML коректно екранований (`&lt;`, `&gt;`, `&amp;`), підсумковий рядок/`⚠️ Закриття пропущено`/`❗ Помилки обробки` присутні в хвості. `build_and_send_digest` у dry-run режимі залогував обидві частини. `task_failure_alert` з реалістичним і повністю порожнім контекстом (`{}`) обидва рази відпрацював без винятку й залогував dry-run повідомлення; текст винятку обрізається рівно на 300 символів до екранування | — |
| 2026-09-16 | E-03 | Розпочато без відповіді на Q4 ("скільки файлів у `raw/` і чи є пропуски") — ROADMAP називає Q2+Q4 передумовою E-03 | Q4 не залежить від коду `replay_courses_dag`: алгоритм §6.2 (синхронізація, бекап, truncate+rebuild в одній транзакції, звіт) повністю визначений специфікацією незалежно від фактичної кількості файлів. Q4 важливий для ІНТЕРПРЕТАЦІЇ майбутнього реального звіту (чи є розриви в історії), а не для написання коду — залишається власнику проєкту, коли MinIO накопичить реальну історію |
| 2026-09-16 | E-03 | `validate_params`/`sync_file_records`/`backup`/`rebuild`/`report` — кожен окрема Airflow-задача (а не один монолітний таск), `rebuild` містить власну "одну зовнішню транзакцію" (TRUNCATE + UPDATE file_record + `process_pending_files(commit_per_file=False)` + ROLLBACK/COMMIT) через окремий `core.db.Session()`, підняту й закриту всередині тіла задачі | SPEC §6.2 явно нумерує 4 окремі кроки (sync/backup/rebuild/report) як задачі DAG; "одна зовнішня транзакція" стосується лише кроку rebuild, а не всього DAG — задачі `sync_file_records`/`backup` комітять самостійно, це узгоджується з §6.2 (backup — окремий `CREATE TABLE ... AS SELECT`, поза транзакцією rebuild) |
| 2026-09-16 | E-03 | Видобування навичок/збагачення (E-01/E-04) не викликається в `rebuild`, лише позначено коментарем-місцем-заповнювачем | Ці пункти ROADMAP ще не реалізовані; SPEC §6.2 п.3 явно каже "якщо вони вже реалізовані" |
| 2026-09-16 | E-03 | Реальний (не dry-run) звіт надсилається через `notify.telegram.send_telegram_message` у основний `TELEGRAM_CHAT_ID`, а не через `notify.alerts.send_alert`/`TELEGRAM_ALERT_CHAT_ID` | Replay-звіт — це операційний підсумок, а не сповіщення про збій; окремі сповіщення захисту під час replay explicitly не надсилаються (§6.2 п.4), тому й основний канал видається доречнішим за alert-канал. Рішення можна переглянути, якщо власник проєкту захоче інше розділення каналів |
| 2026-09-16 | E-03 | Перевірено без Docker: локальний PostgreSQL 16, фейковий S3-клієнт із пагінатором (5 знімків за 3 дні: 2 однакові EPAM, 1 змінений EPAM, 2 однакові SoftServe). `sync_file_records` на чистій БД створює 5 `file_record`, при повторному виклику — `created=0` (ідемпотентність підтверджена, хоча й випадково — через збій попереднього тестового прогону, зафіксовано в БД до відкату). `rebuild(dry_run=true)`: `files_total=5, files_done=5, versions=11, postings=10, errors=[], blocked=[]`, після — `courses`/`load_stats` не змінилися (0/0). `rebuild(dry_run=false)`: ті самі числа, `courses`/`load_stats` після = 11/5 (закомічено); `avg(versions)=1.10 ≪ 5` знімків — відповідає критерію SPEC §14. `backup()` створює `backup_<ts>_courses`/`backup_<ts>_load_stats`, коректно пропускаючи `course_skill`/`course_enrichment` (не існують). Gate `validate_params`: `dry_run=false` без `confirm='REPLAY'` (порожній чи будь-який інший рядок) коректно піднімає `AirflowFailException` в обох випадках | Через відсутність доступу до реального MinIO в цьому середовищі, `sync_file_records`/`rebuild`/`backup` перевірено як окрему логіку (ті самі виклики, що й у тілах задач), а не через живий `airflow dags test` — структуру DAG і відсутність помилок парсингу підтверджено окремо через `DagBag` |
| 2026-09-16 | E-02 | `v_active_courses` створено лише з полів `courses` — без `skills text[]`/полів збагачення | E-01 (`skill`/`course_skill`) і E-04 (`course_enrichment`) ще не реалізовані; SPEC §11 сам відносить skills/enrichment-частину до пунктів E-01/E-02 разом. Оскільки представлення не входять в Alembic і застосовуються ідемпотентним `DROP VIEW ... CASCADE; CREATE VIEW ...`, E-01 просто перезапише `01_active_courses.sql`, додавши `skills`, без потреби в міграції |
| 2026-09-16 | E-02 | `v_posting_episodes`/`v_daily_activity` групують версії в епізоди через `LAG(close_reason)` + кумулятивну суму (`SUM(...) OVER (...)`) замість рекурсивного CTE | Епізод закінчується лише на `close_reason='removed'`; версія, закрита як `'changed'`, залишається в тому самому епізоді. Віконна функція, що інкрементує лічильник епізоду щоразу, як ПОПЕРЕДНЯ версія була `'removed'`, коректно й дешево розв'язує цю задачу "gaps and islands" без рекурсії |
| 2026-09-16 | E-02 | `lifetime_days` рахується проти `MAX(load_stats.snapshot_at)` **джерела** (не пропозиції і не `now()`), `left_censored` — порівнянням `first_seen` з `MIN(load_stats.snapshot_at)` джерела | Точно за формулюванням SPEC §11 ("щоб зупинка пайплайна не завищувала тривалість"); оскільки `active_from`/`active_to` в `storage/loader.py` завжди дорівнюють `file_record.fetched_at` = `load_stats.snapshot_at` того самого файлу, порівняння точне (не наближене за датою) |
| 2026-09-16 | E-02 | Перевірено без Docker: локальний PostgreSQL 16, 5 знімків за 4 дні (9 EPAM: вставлено день1, без змін день2, назва змінена день3, один запис зник день4 — валідно закритий захистом, бо `received=8` не блокується жодним із R1–R3 при цих даних; 1 SoftServe, лише день1). Усі 5 представлень повертають дані; `v_active_courses`=9 (8 EPAM+1 SoftServe, 9-й EPAM закрито); `v_posting_episodes`: 10 епізодів, для закритого запису — `removed_at` встановлено, `versions_count=2`, `is_active=false`, для решти — `is_active=true`, `lifetime_days` рахується проти останнього знімка джерела; критерій "0 рядків із `lifetime_days < 0`" — виконується; `v_daily_activity`: `new=9` день1, `changed=1` день3, `removed=1`/`active_end_of_day` 9→8 день4 — точно збігається з очікуваним; `v_field_changes`: рівно 1 рядок (`title`, день3, саме той запис, де я й змінив назву в синтетичному знімку); `v_pipeline_health`=5 рядків (по одному на файл). `scripts.apply_views` ідемпотентний — повторний запуск не ламає представлення й дані незмінні | — |
| 2026-09-16 | E-01 | `direction_map` побудовано з реального списку `pageProps.filters.Filter.Skills` (40 записів) і фактично використаних `MainSkillStringId` зі знімка I-01, а не вигадано | Значення без чіткого технологічного відповідника (`English`, `InstructionalDesign`, `Chemistry`, `FinancialAnalysisInIt`, `ScientificInformatics`, `SupportServices`, `PerformanceOptimization`) свідомо пропущені, а не змасковані під якусь навичку — "не вигадувати поведінку мовчки" |
| 2026-09-16 | E-01 | Таксономію розширено на 5 навичок поза початковим планом (`REST API`, `RPA`, `Salesforce`, `SAP`, `ServiceNow`), разом 148 навичок | Ці технології прямо підтверджені реальним списком `Filter.Skills` EPAM (`APIsAndIntegration`, `RPA`, `Salesforce`, `SAP`, `ServiceNow`); без них ці напрями `direction_map` лишились би без жодного відображення |
| 2026-09-16 | E-01 | Знайдено й виправлено дефект під час першої ж перевірки на реальних даних: `extract()` може повернути кілька `Match` для ОДНІЄЇ навички (різні аліаси/поля), а `course_skill` має `PRIMARY KEY (course_id, skill_id)` — пряма вставка всіх матчів впала з `UniqueViolation`. Додано чисту функцію `enrich.skills.pick_one_per_skill()`: детерміновано лишає один матч на навичку (пріоритет полів `title > direction > description > tags`, далі за текстом) | Обрано найпростіший спосіб зробити вибір відтворюваним (не залежати від порядку ітерації `set`), не змінюючи схему `course_skill` (яка відповідає SPEC §8.5 буквально) |
| 2026-09-16 | E-01 | `v_skill_weekly`: тиждень визначається через `date_trunc('week', courses.last_seen_at)` | SPEC §11 не деталізує семантику цього представлення (на відміну від `v_posting_episodes`/`v_daily_activity`/`v_field_changes`, для яких є окремий розділ "Семантика"); обрано найпростішу відтворювану метрику — "скільки різних пропозицій підтверджено (знімком) з цією навичкою того тижня" |
| 2026-09-16 | E-01 | `v_skill_pairs`: "остання версія пропозиції" — версія з максимальним `active_from` для `(source, source_id)` незалежно від того, чи вона активна | Формулювання SPEC §11 буквально каже "остання версія", а не "активна версія"; для закритих пропозицій це остання версія перед закриттям |
| 2026-09-16 | E-01 | Перевірено без Docker: локальний PostgreSQL 16, реальні дані I-01 (9 EPAM + 1 SoftServe). `extract_skills`: синхронізовано 148 навичок, оброблено 10 версій курсів. Критерій покриття SPEC §14 — 100.0% активних пропозицій мають хоч одну навичку. `scripts.skills_sample` на всіх 10 записах не показав жодного хибного збігу (перевірено окремо юніт-тестами: Java не збігається всередині JavaScript, C/C# не плутаються, регістрозалежний Go не спрацьовує на "go" в звичайному тексті, R не збігається всередині "Research"). Views: `v_active_courses.skills` коректно агрегує масив; `v_skill_counts`/`v_skill_pairs` дають очікувані числа (AWS/Azure/Docker/Kubernetes із CloudAndDevOps утворюють усі попарні комбінації). `apply_views` з 8 файлами лишається ідемпотентним | — |
| 2026-09-16 | F-03 | Спроба живого прогону через `airflow dags test` (встановлений локально `apache-airflow==3.1.0`, SQLite metadata DB, `SequentialExecutor`, локальний мігрований PostgreSQL, зламаний `CAREERS_URL_BROKEN`) підтвердила лише частину ланцюжка: `fetch_one_source[broken]` реально впала і пішла в `up_for_retry` з реальною затримкою `retry_delay=2 хв`. Прогін зупинено достроково (на запит користувача не чекати ~6+ хв на вичерпання `retries=3`), тому **фактичне спрацювання `finalize` (статус прогону DAG `failed`) наживо не підтверджено** — лише структурно, через `DagBag` (парсинг без помилок, граф задач і `trigger_rule` відповідають SPEC) і пряму перевірку сигнатур `get_current_context`/`AirflowFailException`/`DagRun.get_task_instances` у встановленому пакеті | Не вигадувати результат, якого не бачив: якщо власник проєкту хоче остаточне підтвердження `finalize` на реальних збоях, потрібно самостійно прогнати `docker compose exec airflow-scheduler airflow dags test load_courses_pipeline_dag` (чи дочекатися планового запуску) на повному стеку і перевірити статус прогону в UI/`airflow dags list-runs` |
| 2026-09-16 | E-09 | Реалізовано лише SQL-частину (`metabase/cards/01-12.sql` + `metabase/README.md`); саме створення карток і двох дашбордів у Metabase не виконано | SPEC §12.1 сам каже "поки що створюються вручну в інтерфейсі" — це вимагає живого Metabase, якого немає в цьому середовищі (немає Docker-демона). `metabase/README.md` дає власнику проєкту покрокову інструкцію: яке питання з якого файлу, яка візуалізація, які фільтри дашборду до яких карток прив'язувати |
| 2026-09-16 | E-09 | Додано питання 12 ("Частка відхилених записів") одразу, хоча воно офіційно "після E-05" | E-05 реалізується в тій самій ітерації (запит користувача "реалізуй фазу 2"), тож `load_stats.rejected` вже матиме реальні (не нульові) значення — сенсу відкладати створення запиту нема |
| 2026-09-16 | E-09 | Усі 12 запитів перевірено виконанням проти реальних даних I-01 напряму в Postgres (синтаксис `[[...]]`/`{{...}}` — специфічний для Metabase, тому для тесту прибрано регексом, сама структура запиту незмінна) | Це не замінює перевірку в реальному Metabase (форматування фільтрів, поведінка `[[ ]]`), але підтверджує, що сам SQL синтаксично й семантично коректний і повертає очікувані дані |
| 2026-09-16 | E-04 | Розпочато у фазі 1 сесії разом з E-05, хоча формально це фаза 2 | Користувач явно попросив: "якщо це доцільно і можливо, реалізуй фазу 2". E-04 залежить лише від E-01 (готовий), не потребує live-сервісів поза Postgres/MinIO (які й так емулюються локально в цій сесії) — тому доцільно й можливо |
| 2026-09-16 | E-04 | `city`/`country` наразі майже завжди `None`/`"Ukraine"` для обох джерел (адаптери не витягують місто; країна захардкоджена) — `enrich/geo_aliases.yaml` побудовано наперед, для коли адаптер почне віддавати реальні міста | SPEC §9 сам передбачає це: "Якщо місто треба додатково витягувати в адаптері... це зміна адаптера: збільшити PARSER_VERSION і виконати replay" — інфраструктура класифікації готова, реальна різноманітність міст з'явиться пізніше |
| 2026-09-16 | E-04 | `enrich_courses` обробляє ВСІ версії курсу (не лише активні), за тим самим підходом, що й `extract_skills` (E-01) | SPEC §9 прямо каже "Підхід до версіонування такий самий, як для навичок (RULES_VERSION)" — навички прив'язані до версії; критерій приймання ("кожна активна версія має рядок") виконується автоматично, якщо покрито всі версії |
| 2026-09-16 | E-04 | Перевірено без Docker: локальний PostgreSQL 16, реальні дані I-01 (9 EPAM + 1 SoftServe). Міграція `0005` створює `course_enrichment` з обома CHECK-обмеженнями. `enrich_courses` обробив усі 10 версій; критерій SPEC §14 ("кожна активна версія має рядок у course_enrichment") — 0 пропущених; частка `unknown` для `level_norm`/`format_norm` на цьому наборі — 0.0%/0.0% (усі мали розпізнавані `level`/`course_type`/`format`); `Kiev` у синтетичному тесті нормалізується до `Kyiv`, "Specialization"/"Internship" коректно мапляться на `senior`/`intern` через ключові слова | — |
